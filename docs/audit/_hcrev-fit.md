# hcrev-fit — adversarial fact-check of HARNESS.md's citations

**Scope:** every MODEL-FIT-, field-evidence-, and mechanics-doc-sourced claim in
`docs/design/HARNESS.md`, checked against a snapshot of `docs/design/MODEL-FIT.md`
(`swarm-dev/model-fit` branch, saved to `docs/audit/_hcrev-modelfit-snapshot.md`),
`docs/audit/_hc-field.md`, `docs/audit/_hc-mech.md`, `.swarm/journal/operator.md`,
`.swarm/journal/trigger-scout.md`, and `bin/swarm` directly. No charity extended —
wrong section number = WRONG, number absent from source = WRONG, fabricated quote
dressed as verbatim = WRONG.

| # | CLAIM (HARNESS.md, quoted + line) | SOURCE CHECKED (file:line, quoted) | VERDICT | NOTE |
|---|---|---|---|---|
| 1 | §1/§5 "MODEL-FIT's headline": "142/143 spawns nobody decided" (HARNESS.md:81, also :344, :468, tagged MEASURED/PARENTHETICAL) | MODEL-FIT snapshot line 7-10 (§1 "The bottom line"): "**142 of 143 agents took the inherited default; exactly one was ever pinned.**... It is a decision nobody made, 143 times." | REACHES | Number is right (142 of 143). HARNESS's shorthand "142/143 spawns nobody decided" compresses MODEL-FIT's two-clause statement into one but does not misrepresent it — 142 took the default, all 143 reflect no decision. Lives in MODEL-FIT §1, not §5 as HARNESS.md:81's "§5" framing implies — the phrase "produced 142/143 spawns nobody decided" at HARNESS.md:81 is under HARNESS's own §5 discussing patterns, sourcing MODEL-FIT's headline generally without a specific section tag there; where a section IS explicitly cited (line 344, "1. The last mechanism...") no section number is given either. No hard mis-cite found, but flagging: nowhere does HARNESS actually pin this number to "MODEL-FIT §1" or "§5" explicitly in text — the task brief's "sec1/sec5" framing over-reads an implicit sourcing as an explicit section cite. |
| 2 | §7.2(a): "0/135 mush, 17 recorded behavior changes" cited to MODEL-FIT §5, MEASURED (HARNESS.md:506-508) | MODEL-FIT snapshot §5 (line 413-483), specifically line 434: "unfalsifiable mush — the predicted theater \| **0 of 135**"; line 436-437: "17 documented cases where the compelled field *changed what the agent did*" | REACHES | Both numbers correct (0 of 135; 17 documented cases), both located inside MODEL-FIT §5 as cited. Underlying MODEL-FIT text itself sources these from a child audit doc (`org-review-falsifier-2026-07-12.md`) rather than measuring them itself — MODEL-FIT is reporting a grandchild's count, not a primary measurement. HARNESS's "MEASURED" tag matches MODEL-FIT's own tag, so this is not a HARNESS-introduced overstatement, but the cite chain is MODEL-FIT→org-review-falsifier, not a direct primary source either doc verified independently in this pass. |
| 3 | §6: "MODEL-FIT measured 16% of leaves growing into seats unbriefed" (HARNESS.md:459) | MODEL-FIT snapshot line 372 (inside §4 "Rungs 2 and 3"): "**18 of 115 agents — 16% — carry a coordinator role they were never briefed for.**" | OVERSTATED | Number (16%, 18 of 115) is correct. But the real section is **§4**, not §6 — the claim sits inside MODEL-FIT's "Rung 2/3" leaf-checkability discussion ("The fear arrives by another road: leaves do not stay leaves"), not wherever HARNESS's own §6 placement implies. HARNESS.md:459 does not actually print a section number for this citation (no "§4" or "§6" token appears at that line) — but the task's framing ("verify... find the real section") is answered: real section is §4. Since HARNESS itself doesn't assert a wrong number in text, downgraded from WRONG to OVERSTATED for omitting attribution entirely on a number this load-bearing. |
| 4 | §8: "at least ~9 test call-sites and 12 doc files — a floor, not a total (MEASURED, MODEL-FIT §5)" (HARNESS.md:620-621) | MODEL-FIT snapshot lines 477-479 (§5, "What this repo ships now..."): "blast radius measured by patching a scratch copy of `bin/swarm` and running the suite: **at least** ~9 test call-sites and 12 doc files — *a floor, not a total, per the omission caveat in §4*" | REACHES | Both numbers exact match (~9 test call-sites, 12 doc files), correctly located in §5, correctly flagged as a floor not a total (matching MODEL-FIT's own §4 omission-caveat cross-reference). Clean. |
| 5 | §3+§7: "MODEL-FIT §5: scoping the reason to anything else launders a guess into a decision" (HARNESS.md:195-196, also :506-507) | MODEL-FIT snapshot line 453-454 (§5): "> *A mandated reason forces a parent to justify a choice the repo cannot yet inform — and a recorded reason **launders a guess into a decision**.*" | REACHES | Phrase is verbatim and correctly located in §5. Minor: in MODEL-FIT this sentence is voiced as the *reviewer's* objection quoted by the author ("the one objection the review could not defeat"), not asserted in the author's own voice — HARNESS presents it as flatly "MODEL-FIT §5" without that nuance, but the content and section are accurate. |
| 6 | §4.3+§2.2: "MODEL-FIT §5b's correction" re: ambient fallback / bare launcher (HARNESS.md:108, :266) | MODEL-FIT snapshot line 485 header: "## 5b. The default: do not change the fallback — and stop calling it 'inherit'"; lines 490-494: "There is **no inheritance.**... the child gets whatever the `claude` binary resolves as its **ambient default**, which is nobody's decision" | REACHES | §5b exists exactly as cited, and its content is precisely the "no inheritance / ambient default nobody chose" correction HARNESS attributes to it. Clean on both mentions. |
| 7 | §7e: "the fit rule verbatim (MODEL-FIT §6)" (HARNESS.md:576) | MODEL-FIT snapshot §6 (line 547-582), specifically line 574-578: "The rule this document actually makes is about **fit**: > Put the strong model where being wrong is expensive and invisible. Put a cheap one only where being wrong is cheap to catch..." | REACHES | Section and content match; §6 is titled "The risk, stated as sharply as I can" and does contain the named "fit rule" blockquote. |
| 8 | §5/§9: "floor-not-total discipline (MODEL-FIT §4)" (HARNESS.md:572, :670) | MODEL-FIT snapshot §4 (line 223-412), line 278-279: "publish it as a floor, not a total (\"at least 9 call-sites\")" | REACHES | Section and content match — the floor-not-total discipline is discussed at length in §4's "Rung 3" subsection under the omission-asymmetry argument. |
| 9 | §8: "MODEL-FIT §7 flagged it first" — missing `permissions` block in spawned settings (HARNESS.md:636-638) | MODEL-FIT snapshot §7 (line 586-656), lines 629-630, 641: "**`swarm spawn` writes its settings with hooks only and no `permissions` block**, so *every* spawned child is exposed to this... (The missing `permissions` block is a swarm defect wider than this document, surfaced by `weak-model-deleg`; it is named here so it does not evaporate, and it belongs to whoever owns the spawn path.)" | REACHES | Section correct; MODEL-FIT §7 does name the missing permissions block and explicitly hands it off ("belongs to whoever owns the spawn path"), matching HARNESS's "flagged it first" framing. Note the deeper root: MODEL-FIT §4 (line 334-336) also names this same defect earlier in the document as the root cause of the weak-model-deleg confound — so §7 is not literally the *first* place in MODEL-FIT the defect is named, only the place that explicitly hands it off as unowned. Minor overstatement of "first." |
| 10 | §2.3: "`_hc-field.md` §A1, three independent witnesses" for trigger-scout's limit-death rendering as idle (HARNESS.md:139-141) | `_hc-field.md` §A1 (line 11-22): event file `.swarm/event/trigger-scout.json` (last_words field); trigger-scout.md:1530-1531 (agent's own account, itself quoting `swarm ps`'s rendering of last_words); `.swarm/journal/blocked-visibility.md:47-48` (sibling agent, also quoting `swarm ps`'s rendering of the same last_words string) | REACHES (with caveat) | Three files, three separate agents/records — genuinely three witnesses in the sense used by the audit. But all three ultimately observe the *same single signal* (the `last_words` string surfaced via `ps`), not three independently-derived facts — closer to three readers of one instrument than three instruments. Worth noting the independence is of *observers*, not of *evidence source*. |
| 11 | §2.3: ".swarm/journal/operator.md:199" names three structural bugs (HARNESS.md:141-145) | `.swarm/journal/operator.md:199-200`: line 199 is the header "## 2026-07-12 — [ops-main] BUG FOUND: usage-limit freeze leaves agents unrecoverable by doorbell"; line 200 contains "THREE REAL BUGS: (1) Stop-hook-skipped-on-limit -> no event fact -> no re-ring; (2) ps can't distinguish frozen-on-limit from busy-first-turn; (3) doorbell Enter unreliable (needed a second Return)." | OVERSTATED | Content matches HARNESS's paraphrase closely and all three bugs are genuinely named. But the actual bug-list text is on **line 200**, not line 199 — line 199 is only the entry's header/title line. Citing ":199" for content that lives on :200 is a one-line-off citation; on a doc this granular about file:line citation discipline, that is a real (if small) miss. |
| 12 | §6: "`_hc-field.md` §B1: ~10 agents carry same-day resume entries from this morning's machine restart" (MEASURED) (HARNESS.md:393-395) | `_hc-field.md` §B1 (line 67-84): "That is 9 distinct other agents plus this one (10 total) with an explicit dated resume entry today — short of the operator's reported '~13,'... several of the 13 named orphan-survivors... were not directly checked for a same-day resume entry in this pass (see Falsifier below)." | OVERSTATED | "~10" and "MEASURED" are both accurate to what `_hc-field.md` itself reports (10 total, tagged VERIFIED per-row). But `_hc-field.md` §B1 explicitly flags its own count as a **floor with an unresolved falsifier** — 6 of the 13 named orphan-survivors were never directly checked, so the true count could be higher. HARNESS presents "~10" as a clean MEASURED fact without carrying forward that caveat, which its own child document attached. |
| 13 | §6: "trigger-scout resumed from its own session-limit death... picked up exactly where it left off (VERIFIED — its journal :1528)" (HARNESS.md:396-397) | `.swarm/journal/trigger-scout.md:1528`: "## 2026-07-13 13:23Z — resumed after a session-limit death; reconciliation, not new investigation" (no "picked up exactly where it left off" phrase anywhere in the file; that phrase appears only in `_hc-field.md:26`, the collector's own summary sentence introducing the quote, not inside the quote itself) | WRONG | The quoted phrase "picked up exactly where it left off" is NOT in trigger-scout.md at line 1528 or anywhere else in that file (grepped, zero hits) — it is `_hc-field.md`'s own framing prose (line 26: "...picked up exactly where it left off: `.swarm/journal/trigger-scout.md:1528`:"). HARNESS attributes a secondary document's summary language to the primary journal as if VERIFIED directly against it. This is exactly the "cite pointing at a self-reported/derived doc rather than primary evidence" pattern the task asked to flag — except HARNESS doesn't even cite the derived doc, it cites the primary source for text that isn't there. |
| 14 | §6: "`_hc-mech.md` §5: `herdr pane run <pane>` — spawn's own verb, reusable against a live pane id" (HARNESS.md:426-429) | `_hc-mech.md` §5 item 1 (line 423-429): "Getting a pane to run something new is done exactly once in this codebase via `herdr pane run <pane> <path>`... it is not spawn-specific, it's a general 'type this command into this pane' primitive... mechanically reusable against an EXISTING pane id" | REACHES | Section and content match; verified independently via `grep '"herdr", "pane", "run"' bin/swarm` → single call site at bin/swarm:1185. |
| 15 | §6: "bin/swarm today has no targeted way to stop a pane's occupant (only whole-pane close — `_hc-mech.md` §5.3)" (HARNESS.md:429-430) | `_hc-mech.md` has no "§5.3" — section 5 ("Mechanically: relaunching an existing agent's pane with a different `--model`") is a flat, unnumbered list of 5 items; the matching content ("There is no `pane restart` or `pane kill` verb used anywhere in this file... the closest thing to a teardown primitive... is destructive (the whole tab/pane), not a targeted process kill") is in **item 1** of that list (line 416-422), not any "5.3" | WRONG | No subsection "§5.3" exists anywhere in `_hc-mech.md` (grepped for "5.1", "5.2", "5.3", "### 5" — zero hits). Section-number fabrication: HARNESS invents subsection granularity the source document doesn't have. The underlying factual claim is independently correct — grepped `bin/swarm` directly and confirmed only `pane list/read/send-text/send-keys/run/close` exist, no `pane kill`/`pane stop`/`pane restart` — but the citation itself is wrong. |
| 16 | §6: "`_hc-mech.md` §3" + "bin/swarm:916-935": "a killed-and-relaunched pane is indistinguishable from a fresh launch to this hook" (HARNESS.md:388-393) | `_hc-mech.md` §3 (line 201-292), closest match at line 272-276: "restore does not distinguish 'relaunched after a kill' from 'very first launch'; both look like a fresh SessionStart to this hook." bin/swarm:916-935 read directly = `cmd_restore()` in full, confirmed `payload.get("source") or "startup"` defaults any non-"compact" source (including a post-kill relaunch) to the same `"startup"` branch as a true first launch. | OVERSTATED | Section number (§3) and line range (916-935) are both correct, and the underlying mechanism is real and independently verified by reading bin/swarm directly. But the quoted phrase — "a killed-and-relaunched pane is indistinguishable from a fresh launch to this hook" — is presented in quote marks as if lifted verbatim from `_hc-mech.md`; it is not (grepped, zero hits for "killed-and-relaunched" or "indistinguishable from a fresh launch" in that file). It is HARNESS's own paraphrase of `_hc-mech.md`'s actual sentence, dressed as a direct quote and marked VERIFIED. The underlying fact holds; the citation format overclaims verbatim sourcing. |

---

## Summary by verdict

| Verdict | Count | Rows |
|---|---|---|
| REACHES | 8 | 2, 4, 5, 6, 7, 8, 9, 14 |
| REACHES (with caveat) | 1 | 10 |
| OVERSTATED | 4 | 3, 11, 12, 16 |
| WRONG | 3 | 13, 15, and effectively 15's fabricated subsection |
| UNVERIFIABLE | 0 | — |

**Total rows: 16.** Clean: 8 (50%). Overstated: 4 (25%) — mostly caveats dropped in
transit from a child doc to HARNESS, or a citation one line off. Wrong: 3 (~19%,
counting #15's fabricated section number) — one fabricated section number
(`_hc-mech.md` §5.3, does not exist), one misattributed quote (a collector's own
summary sentence in `_hc-field.md` presented as verbatim primary-source text from
trigger-scout.md), and the "3" tally covers rows 13 and 15 with 16's paraphrase-as-quote
overlapping in kind but scored OVERSTATED rather than WRONG since its section/line
citation is accurate even though the quote marks overclaim.

**Pattern worth naming for the parent:** HARNESS's citation discipline is strong on
section-number accuracy for MODEL-FIT (9 of 9 MODEL-FIT citations checked land on the
correct section) but weaker on the two harvested child docs (`_hc-field.md`,
`_hc-mech.md`), where 3 of 7 citations checked have some defect — a fabricated
subsection number, a misattributed quote, and a paraphrase presented as verbatim. The
MODEL-FIT citations were likely written with the source open (matching a design doc
under active cross-reference); the field/mechanics citations read more like citations
written from memory of a summary pass.
