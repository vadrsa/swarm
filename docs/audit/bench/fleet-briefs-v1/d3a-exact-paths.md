> SUPERSEDED by fleet-briefs-v3/d3a-exact-paths.md (path only — content byte-identical across all 3 versions); kept for the record (lives under the superseded v1/ generation dir).

Produce exactly three files and nothing else in {OUTDIR}:

(1) {OUTDIR}/a-plan.md — a numbered plan of at most 8 steps for reorganizing a
    small library's bookshelf.

(2) {OUTDIR}/b-note.md — one paragraph of 40 to 60 words on why written plans
    drift from what actually happens, containing no code fences and no headings.

(3) {OUTDIR}/c-list.md — a list of exactly 5 items, one per line, and nothing
    else in the file.

Do not create any other files anywhere under {OUTDIR}. When all three exist,
report to your parent {RUNNER} with exactly three lines, one per file, then
stop.
